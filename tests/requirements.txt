pytest
pytest-django
pytest-pythonpath
pytest-xdist

django-callable-perms == 0.1.2
argparse
coverage == 3.7.1
django-dynamic-fixture == 1.8.5
mock == 1.3.0
tox >= 1.8

# django-assets will raise an error if the version numbers
# do not match up exactly.
webassets == 0.11
django-assets == 0.11
